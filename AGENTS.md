# Repository instructions

Before changing this project, read [`AI_context/README.md`](AI_context/README.md) and the context pages it links that are relevant to the task. Treat the implementation and tests as the final source of truth when context documentation differs from code.

Do not modify the repository's `.venv`. It may be used to run existing tools; create a disposable environment under `/tmp` when different dependencies are required.

